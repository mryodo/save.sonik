function res=PsiStat(x, xt)
    %xt(xt==0)=1e-6;
    s=0.5*(abs(x)+abs(xt));
    res=sum(sum(abs(xt).*abs(log(abs(xt./s)))+abs(x).*abs(log(abs(x./s)))));
    if isnan(res)
        
    res=res/sum(sum(xt));
end